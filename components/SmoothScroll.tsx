"use client";
/* Smooth scroll handled by CSS scroll-behavior: smooth on <html>.
   No custom wheel interception — that was breaking GSAP ScrollTrigger. */
export default function SmoothScroll() { return null; }
